{
  "private": true,
  "scripts": {
    "dev": "nodemon",
    "start": "node dist/main.js",
    "build": "tsc src/main.ts --outDir dist",
    "prebuild": "node --eval=\"require('fs').rmdirSync('dist', { recursive: true })\""
  },
  "devDependencies": {
    "nodemon": "2.0.4",
    "typescript": "4.0.3"
  }
}
