{
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only index.ts",
    "dev:notify": "ts-node-dev --notify --respawn --transpile-only index.ts",
    "watch": "ts-node-dev --respawn index.ts"
  },
  "devDependencies": {
    "node-notifier": "9.0.0",
    "ts-node-dev": "1.1.1",
    "typescript": "4.1.5"
  }
}
